id: recall-001
category: recall
name: "Recall exact deployment config"
description: "Agent must recall the specific staging port (7443) — an arbitrary value impossible to guess."
memories:
  - text: "Voltis staging environment: API runs on port 7443 with TLS termination at the Nginx reverse proxy. Production uses port 8443. Staging database is at staging-db.voltis.internal:5432."
    source: eval/recall-001
prompt: "What port does the Voltis staging API run on?"
expected:
  - type: contains
    value: "7443"
    weight: 1.0
