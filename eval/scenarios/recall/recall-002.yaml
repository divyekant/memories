id: recall-002
category: recall
name: "Recall internal naming convention"
description: "Agent must recall the custom 'Vx' prefix for background job classes — completely arbitrary."
memories:
  - text: "Voltis team convention: all background job classes must be prefixed with 'Vx' (e.g., VxEmailSender, VxReportGenerator, VxDataSync). Decided in retro 2026-01-20 to distinguish Voltis jobs from third-party library jobs in the shared Sidekiq dashboard."
    source: eval/recall-002
prompt: "I'm adding a new background job to process CSV imports in the Voltis project. What should I name the job class?"
expected:
  - type: contains
    value: "Vx"
    weight: 1.0
