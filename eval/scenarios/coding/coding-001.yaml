id: coding-001
category: coding
name: "Use project-specific validation library"
description: "Agent must recommend the custom 'vcheck' library — a fictional internal tool Claude cannot know about."
memories:
  - text: "Voltis API validation: Always use the internal 'vcheck' library (voltis.core.vcheck) for request validation, never Pydantic or marshmallow. Apply validation with @vcheck.validate(schema=...) decorator on route handlers. Chosen for performance with our custom error formatting. See ADR-009."
    source: eval/coding-001
prompt: "I need to add input validation to a new Voltis API endpoint. What validation approach should I use?"
expected:
  - type: contains
    value: "vcheck"
    weight: 0.7
  - type: contains
    value: "voltis.core"
    weight: 0.3
