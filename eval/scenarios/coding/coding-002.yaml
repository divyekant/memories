id: coding-002
category: coding
name: "Apply critical migration flag"
description: "Agent must recall the exact --lock-timeout=5000 flag — a specific value impossible to derive."
memories:
  - text: "CRITICAL: Voltis migration 047_add_invoice_index must be run with --lock-timeout=5000 flag. Without this flag, it acquires an ACCESS EXCLUSIVE lock on the invoices table (12M+ rows) causing a full outage. Discovered during 23-minute outage on 2026-02-14 (postmortem PM-2026-003)."
    source: eval/coding-002
prompt: "I need to run Voltis database migration 047_add_invoice_index in production. Anything I should know?"
expected:
  - type: contains
    value: "lock-timeout"
    weight: 0.4
  - type: contains
    value: "5000"
    weight: 0.3
  - type: contains
    value: "PM-2026-003"
    weight: 0.3
